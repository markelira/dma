// @ts-nocheck
/* eslint-disable */
/* This file was generated by Unframer for Framer project cfe7f02456486a1b "TaskFlow (copy)", do not edit manually */
"use client";

/**
 * @typedef Locale
 * string
 */

/**
 * @typedef {{
 * children?: React.ReactNode
 * locale?: Locale
 * style?: React.CSSProperties
 * className?: string
 * id?: string
 * ref?: any
 * width?: any
 * height?: any
 * layoutId?: string
}} Props

 */
import { routes } from "./chunks/chunk-UZZEI6KP.js";

// virtual:animated-counter
import { Fragment } from "react";
import { ContextProviders } from "unframer";

// /:https://framerusercontent.com/modules/eLDAMIHYWJ7MeBWvvxrm/6ZBqcp1mpeuW6pIpWVUF/Animated_Counter_3.js
import { jsx as _jsx } from "react/jsx-runtime";
import { addPropertyControls, ControlType } from "unframer";
import { useState, useEffect } from "react";
import { useSpring, animated } from "react-spring";
import { useInView } from "react-intersection-observer";
function Animated_Counter({ font, number, duration, attachment, color }) {
	const [isInView, setIsInView] = useState(false);
	const { ref, inView } = useInView({
		threshold: 0.5,
	});
	useEffect(() => {
		setIsInView(inView);
	}, [inView]);
	const { count } = useSpring({
		from: {
			count: 0,
		},
		to: {
			count: isInView ? number : 0,
		},
		config: {
			duration,
		},
	});
	return (
		<div
			ref={ref}
			style={{
				...font,
				color,
			}}
		>
			<animated.div>
				{count.interpolate((value) => `${value.toFixed(0)}${attachment}`)}
			</animated.div>
		</div>
	);
}
addPropertyControls(Animated_Counter, {
	number: {
		title: "Number",
		type: ControlType.Number,
		defaultValue: 100,
		displayStepper: true,
	},
	attachment: {
		title: "Attachment",
		type: ControlType.String,
		description: "Attach something at the end",
		defaultValue: "%",
	},
	duration: {
		title: "Duration",
		type: ControlType.Number,
		defaultValue: 1e3,
		displayStepper: true,
	},
	font: {
		// @ts-ignore
		type: ControlType.Font,
		title: "Font",
		controls: "extended",
		displayFontSize: true,
		displayTextAlignment: false,
		defaultFontType: "sans-serif",
		defaultValue: {
			fontSize: 24,
			lineHeight: "1.4em",
		},
	},
	color: {
		type: ControlType.Color,
		title: "Color",
	},
});

// virtual:animated-counter
import { WithFramerBreakpoints } from "unframer";
import { jsx } from "react/jsx-runtime";
var locales = [];
var defaultResponsiveVariants = {};
/** @type {function(Props): any} */
function ComponentWithRoot({ locale, ...rest }) {
	return (
		<ContextProviders
			routes={routes}
			framerSiteId={
				"cfe7f02456486a1b782c98a22db14254d4e15a2a92102e54c71e84b8efc93bf4"
			}
			locale={locale}
			locales={locales}
		>
			<Animated_Counter {...rest} />
		</ContextProviders>
	);
}
/**
 * @type {import("unframer").UnframerBreakpoint}
 * Represents a responsive breakpoint for unframer.
 */

/**
 * @typedef VariantsMap
 * Partial record of UnframerBreakpoint to Props.variant, with a mandatory 'base' key.
 * { [key in UnframerBreakpoint]?: Props['variant'] } & { base: Props['variant'] }
 */

/**
 * Renders AnimatedCounterFramerComponent for all breakpoints with a variants map. Variant prop is inferred per breakpoint.
 * @function
 * @param {Omit<Props, 'variant'> & {variants?: VariantsMap}} props
 * @returns {any}
 */
ComponentWithRoot.Responsive = ({ locale = "", ...rest }) => {
	return (
		<ContextProviders
			routes={routes}
			framerSiteId={
				"cfe7f02456486a1b782c98a22db14254d4e15a2a92102e54c71e84b8efc93bf4"
			}
			locale={locale}
			locales={locales}
		>
			<WithFramerBreakpoints
				Component={Animated_Counter}
				variants={defaultResponsiveVariants}
				{...rest}
			/>
		</ContextProviders>
	);
};
Object.assign(ComponentWithRoot, Animated_Counter);
var animated_counter_default = ComponentWithRoot;
export { animated_counter_default as default };
